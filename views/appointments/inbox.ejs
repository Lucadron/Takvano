<ul class="nav nav-tabs mb-3 justify-content-center" role="tablist">
  <li class="nav-item">
    <a class="nav-link active" data-bs-toggle="tab" href="#aktif" role="tab">🟢 Aktif Randevular</a>
  </li>
  <li class="nav-item">
    <a class="nav-link" data-bs-toggle="tab" href="#gecmis" role="tab">🔙 Geçmiş Randevular</a>
  </li>
</ul>

<div class="tab-content">
  <div class="tab-pane fade show active" id="aktif" role="tabpanel">
    <% if (aktif.length === 0) { %>
      <p class="text-muted text-center">Aktif randevunuz yok.</p>
    <% } else { %>
      <%- include("./_organizeTable", { talepler: aktif }) %>
    <% } %>
  </div>

  <div class="tab-pane fade" id="gecmis" role="tabpanel">
    <% if (gecmis.length === 0) { %>
      <p class="text-muted text-center">Geçmiş randevunuz yok.</p>
    <% } else { %>
      <%- include("./_organizeTable", { talepler: gecmis }) %>
    <% } %>
  </div>
</div>
